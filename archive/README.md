# Archive

This directory contains deprecated files kept for reference.

## cryptoscope.html

Legacy single-file prototype of the CryptoScope frontend.
The current UI is the Vite + React app at `app/frontend/`.
This file is no longer maintained.

## dashboard.py

Legacy Streamlit dashboard (Phase 7).
Replaced by the React frontend which uses real pipeline data via `export_json.py`.
Kept here for reference only.
